services:
  frontend:
    image: ${CI_REGISTRY_IMAGE/frontend}:${image_tag}
    networks:
      - traefik-public
    deploy:
      mode: replicated
      replicas: 1
      labels:
        - "traefik.enable=true"
        - "traefik.constraint-label=traefik-public"
        - "traefik.swarm.network=traefik-public"

        - "traefik.http.routers.${stack-name}-http.rule=Host(`${stack_name}.doe25.swarm.chas-lab.dev`)"
        - "traefik.http.routers.${stack-name}-http.entrypoints=http"
        - "traefik.http.routers.${stack-name}-http.middlewares=https-redirect"

        - "traefik.http.routers.${stack-name}-https.rule=Host(`${stack_name}.doe25.swarm.chas-lab.dev`)"
        - "traefik.http.routers.${stack-name}-https.entrypoints=https"
        - "traefik.http.routers.${stack-name}-https.tls=true"
        - "traefik.http.routers.${stack-name}-https.tls.certresolver=le"

        - "traefik.http.services.${stack-name}.loadbalancer.server.port=5173"

  backend:
    image: ${CI_REGISTRY_IMAGE/backend}:${image_tag}
    networks:
      - traefik-public
    deploy:
      mode: replicated
      replicas: 1
      labels:
        - "traefik.enable=true"
        - "traefik.constraint-label=traefik-public"
        - "traefik.swarm.network=traefik-public"

        - "traefik.http.routers.${stack-name}-http.rule=Host(`${stack_name}.doe25.swarm.chas-lab.dev`)"
        - "traefik.http.routers.${stack-name}-http.entrypoints=http"
        - "traefik.http.routers.${stack-name}-http.middlewares=https-redirect"

        - "traefik.http.routers.${stack-name}-https.rule=Host(`${stack_name}.doe25.swarm.chas-lab.dev`)"
        - "traefik.http.routers.${stack-name}-https.entrypoints=https"
        - "traefik.http.routers.${stack-name}-https.tls=true"
        - "traefik.http.routers.${stack-name}-https.tls.certresolver=le"

        - "traefik.http.services.${stack-name}.loadbalancer.server.port=3000"


networks:
  traefik-public:
    external: true

